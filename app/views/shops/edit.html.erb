<% if logged_in? %>
  <% set_meta_tags :title => 'SHOP情報編集', :reverse => true %>
  <div class="container">
    <div class="new">
      <h1>編集画面</h1>

      <div class="row">
        <div class="col-md-6 col-md-offset-3 col-xs-12">
          <%= render 'form' %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <%= render 'layouts/notfound' %>
<% end %>
