<% if logged_in? %>
  <% provide(:title, 'SHOP情報編集') %>
  <div class="container">
    <div class="new">
      <h1>編集画面</h1>

      <div class="row">
        <div class="col-md-6 col-md-offset-3 col-xs-12">
          <%= render 'form' %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <%= render 'layouts/notfound' %>
<% end %>
